---
- name: Deploy kernel hardening configuration
  ansible.builtin.template:
    src: 99-security-baseline.conf
    dest: /etc/sysctl.d/99-security-baseline.conf
    owner: root
    group: root
    mode: "0644"
  notify: Reload sysctl